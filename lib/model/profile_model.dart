import 'package:invoice_management/imports.dart';

class ProfileModel {
  final String? title;
  final IconData? icon;
  final Color? bgColor;
  final Color? textColor;

  ProfileModel({this.title, this.icon, this.bgColor, this.textColor});
}
